apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-tags-live
  namespace: argocd
spec:
  project: live-media

  source:
    repoURL: https://gitlab.com/technologycompany/mediashare.git
    targetRevision: k8s
    path: k8s/dev/tags-svc/live
  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    syncOptions:
      - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
